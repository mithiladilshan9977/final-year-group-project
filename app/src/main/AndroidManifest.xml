<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
<!--    foodmart539@gmail.com-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31" >
        <activity
            android:name=".officersplashactivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".splashScreen"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".ShowDriverChatPage"
            android:exported="false" />
        <activity
            android:name=".historySingelActivity"
            android:exported="false" />
        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplicationNoActionBar" />
        <activity
            android:name=".driverSetting"
            android:exported="false"
            android:theme="@style/Theme.MyApplicationNoActionBar" />
        <activity
            android:name=".customerSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplicationNoActionBar" />
        <activity
            android:name=".customerMapActivity"
            android:exported="false" />
        <activity
            android:name=".customerLoginActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplicationNoActionBar" />
        <activity
            android:name=".DriverLogInActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplicationNoActionBar"/>
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB2LbzPvry2QTzMZI_ZNihy374XkpcWi58" />

        <activity
            android:name=".DriverMapsActivity"
            android:theme="@style/Theme.MyApplication"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MyApplicationNoActionBar"  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>